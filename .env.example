# LLM provider: "api" (OpenAI/Gemini) or "local" (Ollama/vLLM)
LLM_PROVIDER=api

# API keys (when LLM_PROVIDER=api)
OPENAI_API_KEY=
GEMINI_API_KEY=

# Local LLM (when LLM_PROVIDER=local)
OLLAMA_BASE_URL=http://localhost:11434

# Local ASR
WHISPER_MODEL_SIZE=base
